# Copy this file to .env and fill in your own values. .env is gitignored — never commit it.

# OpenRouter — one key, one client, routes to the full model roster (see PLAN.md
# "Infrastructure" section). Get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=
